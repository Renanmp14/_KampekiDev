PORT=3001
NODE_ENV=development

JWT_SECRET=troque_por_string_aleatoria_longa_aqui
JWT_EXPIRES_IN=7d

ADMIN_EMAIL=admin@seudominio.local
ADMIN_PASSWORD=defina_uma_senha

# --- Usuários adicionais (OPCIONAL) ---------------------------------------
# Sem esta variável, vale só o ADMIN_EMAIL/ADMIN_PASSWORD acima, tratado como
# perfil "admin" — é o comportamento de sempre, e o que o app desktop usa.
#
# Com ela, é possível ter VÁRIOS usuários, cada um com um perfil:
#   admin   -> faz tudo (lançar, editar, excluir, importar)
#   leitura -> só consulta; qualquer POST/PUT/DELETE é recusado com 403
# O ADMIN_EMAIL continua valendo mesmo com a lista preenchida.
#
# Precisa ser um JSON em UMA ÚNICA LINHA:
# USUARIOS_JSON=[{"email":"gestor@seudominio.local","senha":"...","perfil":"admin"},{"email":"consulta@seudominio.local","senha":"...","perfil":"leitura"}]
#
# ATENÇÃO: a trava só é uma restrição de verdade na VERSÃO WEB, onde o backend
# roda no servidor. No app desktop o backend roda na máquina do próprio usuário,
# que consegue ler e editar este arquivo — ali o perfil evita engano, não fraude.

GOOGLE_SHEET_ID=coloque_o_id_da_planilha

# JSON da Service Account em UMA ÚNICA LINHA (todo o conteúdo do arquivo de chave
# que o Google Cloud gera). NUNCA cole a chave real aqui — este arquivo é
# versionado. A chave real vai só no .env (ignorado pelo git).
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"seu-projeto","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"sua-conta@seu-projeto.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"...","universe_domain":"googleapis.com"}

GROWTH_ALERT_THRESHOLD=20
